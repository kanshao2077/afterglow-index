# Design QA - 余光档案 MVP

日期：2026-09-08

视觉真值：最终选定的黑色复古未来主义 mock；GenLumio 只承担「介绍 → 筛选维度 → 二级筛选 → 作品瀑布流」的信息架构参考。用户最新指定的中文动态标题覆盖了 mock 中的英文标题。

## 桌面视觉对照

- 视口：1487 × 1058。
- 页面左右边距：38px，与目标一致。
- 首屏结构：品牌、动态大标题、右侧光轨、四个筛选维度、二级筛选、四列开放式作品网格完整保留。
- 网格起点：约 474px；目标约 459px，15px 差异来自中文标题字面高度，首屏仍能看到第一排完整作品及第二排开头。
- 四列内容无卡片外壳、无玻璃效果、无大圆角；列间距 26px。
- 主标题固定读取全站作品数，当前为「51 件作品，构成生成时代的视觉档案」；字体使用暖白、轻微描边、低对比模糊与冷色残影形成磨砂感，未牺牲可读性。
- 光轨使用原创 1440 × 360 栅格资产，没有使用 CSS 绘图或热链参考站素材。
- 作品封面全部在抓取阶段下载到本地；51/51 文件存在，前端远程热链数为 0。
- 删除了没有信息价值的卡片序号叠层；颜色收敛为一套冷蓝交互色，奖项与来源信息回到中性色。

## 响应式

| 视口 | 列数 | 横向溢出 |
|---|---:|---:|
| 1366 × 768 | 4 | 0 |
| 1024 × 768 | 3 | 0 |
| 768 × 1024 | 2 | 0 |
| 390 × 844 | 1 | 0 |

移动端标题有意识地排为三行，避免把 11 个中文字硬挤成不可读的小字；筛选维度和选项可横向滚动，搜索按钮点击宽度 58px。

## 核心交互

- 编辑精选筛选：0 张卡片，并显示明确空状态。
- 机器发现筛选：51 张卡片。
- 来源筛选：6 个来源族，支持按奖项、平台与里程碑来源切换。
- 搜索 `Total Pixel`：1 张结果。
- 点击作品：详情弹窗可见。
- 获奖作品详情：官方证据入口可见。
- 关闭弹窗：打开的 dialog 数回到 0。
- 浏览器 console warning/error：0。

## 状态与无障碍

- 包含读取中、目录失败、筛选为空和正常成功状态。
- 主筛选使用 tab 语义，二级筛选使用 `aria-pressed`，结果数使用 `aria-live`。
- 键盘 focus 有 2px 蓝色轮廓；系统减少动态效果时关闭图片缩放动画。
- 详情使用原生 modal dialog，支持浏览器 Escape 行为和背景关闭。
- 读取状态改为与四列网格同构的骨架屏；按钮补充按压反馈。

## 发布路径

- GitHub Pages 构建使用 `/afterglow-index/` 基路径。
- 页面入口、脚本、样式与目录 JSON 在本地 Pages 预览中均返回 200。
- `npm run test:catalog` 校验多来源数量、每周范围、热门代表作、去重和本地封面；`npm run test:sites` 校验静态资源与回退逻辑。

## 结论

未发现 P0、P1 或 P2 问题。

final result: passed
